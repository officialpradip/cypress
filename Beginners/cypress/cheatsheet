npx cypress open # to run cypress
npx cypress run # run in terminal
npx cypress run --headed #run in terminal with headed
npx cypress run --spec cypress\e2e\firsttest.cy.js #run specific file
npx cypress run --browser chrome #to switch browser as default in electron

###CSSLocator
tag id ---- #id -->input tags #
tag class -----.class
tag attribute ---- [attribute='value]
tag class attribute --- .class[attribute='value']

###Xpath
npm install -D cypress-xpath
add refernces in command files inside support folder
